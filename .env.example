# Signer backend: memory (local keypair) or keychain (Privy / Turnkey).
SIGNER_BACKEND=memory
KEYCHAIN_BACKEND=

# Memory signer (default).
OWNER_KEYPAIR=./secrets/owner.json

# Privy keychain signer.
PRIVY_APP_ID=
PRIVY_APP_SECRET=
PRIVY_WALLET_ID=
PRIVY_WALLET_ADDRESS=

# Turnkey keychain signer.
TURNKEY_API_PUBLIC_KEY=
TURNKEY_API_PRIVATE_KEY=
TURNKEY_ORGANIZATION_ID=
TURNKEY_WALLET_ADDRESS=

# Runtime layer: same wallet signs on-chain txs and gateway authSig.
# Leave GATEWAY_ENDPOINTS unset to use the default Molpha gateway
# (https://dev-gateway.molpha.io). Must not be a Solana RPC URL.
GATEWAY_ENDPOINTS=
# Base58 authority of each gateway above, comma-separated in the same order.
# Request signatures bind the gateway's PDA, so gateways that do not serve
# GET /v1/info need this; leave an entry empty to discover it from /v1/info.
GATEWAY_AUTHORITIES=
SOLANA_RPC=https://api.devnet.solana.com

# Optional runtime pins.
MOLPHA_EVM_NETWORKS=evm-sepolia
MOLPHA_STARKNET_NETWORKS=starknet-sepolia

# Guardrails (server-side, v1).
MOLPHA_MAX_EXECUTES_PER_DAY=100
MOLPHA_DRY_RUN=false

# x402 pay-per-request (execute_agent_round): each round is paid with an x402
# USDC transfer from this wallet to the gateway authority (GATEWAY_AUTHORITIES).
# Caps are decimal USDC (6 decimals); the MCP refuses to pay for a round above
# MOLPHA_X402_MAX_PRICE_USDC or once MOLPHA_X402_MAX_SPEND_PER_DAY_USDC has been
# signed for the day.
MOLPHA_X402_MAX_PRICE_USDC=1
MOLPHA_X402_MAX_SPEND_PER_DAY_USDC=10

# Bootstrap CLI only (subscribe/extend — debits USDC).
MOLPHA_PLAN=Basic
MAX_PRICE_USDC=
