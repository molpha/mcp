provenance=true
# @molpha/sdk is a file: dependency on ../sdk. Install it as a copy, not a symlink, so it
# shares this package's @solana/web3.js and the signers' instanceof checks hold.
install-links=true
