---
"@molpha/mcp": minor
---

Add stateless Streamable HTTP mode with `molpha-mcp --http`, per-request Privy/Turnkey signing headers, and unsigned discovery, reads, and x402 quotes. Hosted defaults reject plaintext encryption inputs, retain per-round price limits, disable shared daily budgets, enforce IP rate limits, and sanitize errors and logs. Include a container and deployment runbook; stdio remains the default.
